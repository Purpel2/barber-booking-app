datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL") // To znowu jest dozwolone w v6
}

generator client {
  provider = "prisma-client-js"
}

model TestService {
  id    String @id @default(uuid())
  title String
  price Int
}